### 环境配置
在当前项目路径下
1. ` conda create -n dp_lang python==3.10 -y `
2. ` conda activate dp_lang `
3. ` pip install lerobot `
4. ` pip install -e . `
5. ` pip install tensorboard websockets msgpack `
6. ` pip install transformers==4.57.0 `
7. ` conda install ffmpeg=7.1.1 -c conda-forge `